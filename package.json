{
  "name": "js-comprehensive-info",
  "version": "1.0.0",
  "description": "Comprehensive system, git, and optional Java info for GitHub Actions.",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --source-map --license licenses.txt",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github",
    "actions",
    "info",
    "system",
    "git"
  ],
  "author": "Your Name/Org",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
